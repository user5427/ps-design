name: BiomeFrontend

on:
  pull_request:
    branches: [ main, master ]

jobs:
  biome-format:
    # Skip if the last commit was made by the bot to prevent infinite loops
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.user.login != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Biome formatter
        run: npm run fmt:frontend

      - name: Run Biome lint with auto-fix
        run: npm run lint-fix:frontend

      - name: Commit formatted files
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git checkout ${{ github.head_ref }}

          git add -A

          git diff --cached --quiet || git commit -m "Apply Frontend Biome linting and formatting"

          git push origin ${{ github.head_ref }}

      - name: Validate lint
        run: npm run lint:frontend
