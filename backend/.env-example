## Backend .env example
# Copy this file to .env and fill in values.
# DATABASE_URL: Postgres connection string used by Prisma
# PORT: Fastify server port (match frontend VITE_BACKEND_PORT for proxy)
# FASTIFY_GRACEFUL_SHUTDOWN_DELAY: milliseconds delay before forced shutdown
# RATE_LIMIT_MAX: optional per-minute limit (defaults to 100 if omitted)
DATABASE_URL=postgres://user:password@host:5432/dbname
PORT=4000
FASTIFY_GRACEFUL_SHUTDOWN_DELAY=500
RATE_LIMIT_MAX=100